# tell them to pick a num
num = input("(Pick a random number!)")
# define the function
def is_even(num):
# determine whether num is even or # not
    if is_even(num)
        print("(True")
        return True
    else:
        print("(False")
        return False
is_even(num)
